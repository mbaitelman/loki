Description=Promtail service
After=network.target

[Service]
Type=simple
User=promtail
ExecStart=<%=@promtail_bin%> -config.file /etc/promtail/config/config-promtail.yml

[Install]
WantedBy=multi-user.target